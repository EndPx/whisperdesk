# Claude Code — personal, jangan di-commit
CLAUDE.local.md
.claude/settings.local.json

# Secrets
.env
.env.*

# Dependencies & artifacts (aktifkan sesuai stack nanti)
node_modules/
out/
cache/
dist/

# Claude Code project config — local only, jangan ke GitHub
CLAUDE.md
.claude/

# Derived/tooling artifacts — local only
graphify-out/
.obsidian/

# UI dev artifacts
.playwright-mcp/
whisperdesk-*.jpeg
flow*.jpeg
sim*.jpeg

# internal ops docs — kept local, not published (public docs live in the README + GitBook)
docs/

# Cygwin/Git-Bash crash dumps
*.stackdump
